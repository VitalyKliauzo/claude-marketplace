# Plugin Three — Placeholder

**Rename this folder before submitting.** Pick a name that reflects what your plugin does.

## What to do here

1. **Rename the folder** — replace `plugin-three` with a meaningful name.
2. **Update `.claude-plugin/plugin.json`** — set `name`, `description`, `keywords`, `author`.
3. **Add components** based on what your plugin does:
   - `commands/` — custom slash commands
   - `hooks/` — hook configuration + scripts
   - `skills/<skill-name>/SKILL.md` — skills
   - `agents/` — subagent definitions
   - `.mcp.json` — MCP server configuration
4. **Update `../../.claude-plugin/marketplace.json`** to reflect the new name and path.
5. **Delete this README** once your plugin is filled in.

See the root [README.md](../../README.md) for full requirements.
